# Tests de integración de `agent/`

Tests que cruzan varios módulos de `packages/runtime` (por ejemplo, un run completo contra un
`OllamaProvider` fake), distinguidos con doble guion bajo de los tests unitarios colocados junto a cada
archivo (`docs/architecture/02-estructura-de-carpetas.md` §3).

Placeholder de esqueleto: no hay tests de integración todavía; se agregan junto con `RunController` en una
fase posterior.
