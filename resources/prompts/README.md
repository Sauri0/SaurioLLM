# Prompts del sistema

System prompts por rol en Markdown (`lead.md`, `coder.md`, `reviewer.md`, `explorer.md`, ...) + few-shot en
JSON; su hash entra en `effective_config_json` (`docs/architecture/02-estructura-de-carpetas.md` §1).

Placeholder de esqueleto: todavía no hay prompts reales; se agregan en una fase posterior de implementación.
